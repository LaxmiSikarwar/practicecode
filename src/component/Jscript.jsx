import React from 'react'

export const Jscrip = () => {

    const Array =['name']
  return (
    <div>
        <h1>{Array.length===0 && 'emapty arrray'}</h1>
        <h1>Array of length :{Array.length}</h1>
    </div>
  )
}
